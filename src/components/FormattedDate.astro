---
interface Props {
  date: Date;
}

const { date } = Astro.props;
const formattedDate = date
  .toLocaleDateString('en-us', {
    year: 'numeric',
    month: 'short',
    day: '2-digit',
  })
  .replace(',', '');
---

{formattedDate}
